
<tr>
  <td><%= question.id %></td>
  <td><%= question.body %></td>
  <td><%= link_to :show, question_path(question.id) %></td>
  <td><%= link_to :edit, edit_question_path(question) %></td>
  <td><%= link_to :destroy, question, data: { method: :delete, confirm: "Are you sure?" } %></td>
</tr>
